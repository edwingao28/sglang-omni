name: S2-Pro CI (v1)

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: s2pro-ci-v1-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

# DAG:
#   docs ──► stage-1-non-streaming ──┐
#        └─► stage-2-streaming ──────┤
#                                    └──► stage-3-consistency

jobs:
  docs:
    name: docs (v1)
    runs-on: [self-hosted]
    timeout-minutes: 10
    container:
      image: frankleeeee/sglang-omni:dev
      options: --gpus all --rm -v /dev/shm:/dev/shm
    env:
      VENV_NAME: omni-s2pro-v1
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ./.github/actions/omni-pipeline-env
        with:
          pipeline-version: v1

      - uses: ./.github/actions/omni-setup
        with:
          venv-name: ${{ env.VENV_NAME }}
          install-deps: "true"

      - name: Run docs tests
        shell: bash
        run: |
          source "${VENV_NAME}/bin/activate"
          pytest tests/docs/s2pro/test_docs_tts_s2pro.py -v -s -x
        env:
          HF_ENDPOINT: https://hf-mirror.com

      - name: Post-stage cleanup
        if: always()
        uses: ./.github/actions/omni-post-stage
        with:
          stage-label: S2-Pro docs (v1)

      - name: Save cache
        if: always()
        shell: bash
        run: |
          if "${VENV_NAME}/bin/python" -c "import torch" 2>/dev/null; then
            rm -rf "/github/home/${VENV_NAME}"
            cp -p -r "${VENV_NAME}" /github/home/
          else
            echo "::warning::Skipping cache save — venv appears corrupted (torch not importable)"
          fi

  stage-1-non-streaming:
    name: stage 1 - non-streaming (v1)
    needs: docs
    runs-on: [self-hosted]
    timeout-minutes: 5
    container:
      image: frankleeeee/sglang-omni:dev
      options: --gpus all --rm -v /dev/shm:/dev/shm
    env:
      VENV_NAME: omni-s2pro-v1
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ./.github/actions/omni-pipeline-env
        with:
          pipeline-version: v1

      - uses: ./.github/actions/omni-setup
        with:
          venv-name: ${{ env.VENV_NAME }}

      - name: Run benchmark stage
        shell: bash
        run: |
          source "${VENV_NAME}/bin/activate"
          pytest tests/test_model/test_s2pro_tts_ci.py -v -s -x --concurrency 8 --s2pro-stage s2pro-stage-1-nonstream
        env:
          HF_ENDPOINT: https://hf-mirror.com
          SGLANG_SEEDTTS50_DIR: /github/home/seedtts-50
          S2PRO_STAGE_OUTPUT_ROOT: ${{ github.workspace }}/stage-results/nonstream

      - name: Post-stage cleanup
        if: always()
        uses: ./.github/actions/omni-post-stage
        with:
          stage-label: S2-Pro stage 1 (non-streaming, v1)
          artifact-search-root: stage-results/nonstream
          artifact-path-globs: |
            */speed_results.json
            */wer_results.json
          artifact-upload-name: s2pro-stage-1-results-v1
          artifact-upload-path: stage-results/nonstream
          artifact-if-no-files-found: error

  stage-2-streaming:
    name: stage 2 - streaming (v1)
    needs: docs
    runs-on: [self-hosted]
    timeout-minutes: 5
    container:
      image: frankleeeee/sglang-omni:dev
      options: --gpus all --rm -v /dev/shm:/dev/shm
    env:
      VENV_NAME: omni-s2pro-v1
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ./.github/actions/omni-pipeline-env
        with:
          pipeline-version: v1

      - uses: ./.github/actions/omni-setup
        with:
          venv-name: ${{ env.VENV_NAME }}

      - name: Run benchmark stage
        shell: bash
        run: |
          source "${VENV_NAME}/bin/activate"
          pytest tests/test_model/test_s2pro_tts_ci.py -v -s -x --concurrency 8 --s2pro-stage s2pro-stage-2-stream
        env:
          HF_ENDPOINT: https://hf-mirror.com
          SGLANG_SEEDTTS50_DIR: /github/home/seedtts-50
          S2PRO_STAGE_OUTPUT_ROOT: ${{ github.workspace }}/stage-results/stream

      - name: Post-stage cleanup
        if: always()
        uses: ./.github/actions/omni-post-stage
        with:
          stage-label: S2-Pro stage 2 (streaming, v1)
          artifact-search-root: stage-results/stream
          artifact-path-globs: |
            */speed_results.json
            */wer_results.json
          artifact-upload-name: s2pro-stage-2-results-v1
          artifact-upload-path: stage-results/stream
          artifact-if-no-files-found: error

  stage-3-consistency:
    name: stage 3 - consistency (v1)
    needs: [stage-1-non-streaming, stage-2-streaming]
    runs-on: [self-hosted]
    timeout-minutes: 5
    container:
      image: frankleeeee/sglang-omni:dev
      options: --gpus all --rm -v /dev/shm:/dev/shm
    env:
      VENV_NAME: omni-s2pro-v1
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ./.github/actions/omni-pipeline-env
        with:
          pipeline-version: v1

      - uses: ./.github/actions/omni-setup
        with:
          venv-name: ${{ env.VENV_NAME }}

      - name: Download stage 1 artifact
        uses: actions/download-artifact@v4
        with:
          name: s2pro-stage-1-results-v1
          path: stage-artifacts/nonstream

      - name: Download stage 2 artifact
        uses: actions/download-artifact@v4
        with:
          name: s2pro-stage-2-results-v1
          path: stage-artifacts/stream

      - name: Run benchmark stage
        shell: bash
        run: |
          source "${VENV_NAME}/bin/activate"
          pytest tests/test_model/test_s2pro_tts_ci.py -v -s -x --concurrency 8 --s2pro-stage s2pro-stage-3-consistency
        env:
          HF_ENDPOINT: https://hf-mirror.com
          SGLANG_SEEDTTS50_DIR: /github/home/seedtts-50
          S2PRO_STAGE1_SPEED_RESULTS_DIR: ${{ github.workspace }}/stage-artifacts/nonstream
          S2PRO_STAGE2_SPEED_RESULTS_DIR: ${{ github.workspace }}/stage-artifacts/stream

      - name: Post-stage cleanup
        if: always()
        uses: ./.github/actions/omni-post-stage
        with:
          stage-label: S2-Pro stage 3 (consistency inputs, v1)
          artifact-search-root: stage-artifacts
          artifact-path-globs: |
            */speed_results.json
            */wer_results.json
